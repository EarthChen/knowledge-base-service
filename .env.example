# Knowledge Base Service Configuration

# Server
HOST=0.0.0.0
PORT=8100
LOG_LEVEL=INFO

# FalkorDB
FALKORDB__HOST=falkordb
FALKORDB__PORT=6379
FALKORDB_PASSWORD=

# FalkorDB external port (docker-compose only)
FALKORDB_PORT=6379

# Embedding model (bge-m3: multilingual+code, 568M params, 8192 context, 1024 dim)
# backend: "onnx" (lightweight) or "torch" (full PyTorch, supports MPS/CUDA)
EMBEDDING__MODEL_NAME=BAAI/bge-m3
EMBEDDING__DIMENSION=1024
EMBEDDING__DEVICE=auto
EMBEDDING__BACKEND=onnx
EMBEDDING__ONNX_PATH=
EMBEDDING__BATCH_SIZE=32
EMBEDDING__CHUNK_SIZE=64
EMBEDDING__USE_FP16=true
EMBEDDING__MAX_LENGTH=8192
EMBEDDING__QUERY_PREFIX=

# Git / GitLab (for remote repository indexing via git_url)
GIT__GITLAB_URL=https://git.example.com
GIT__GITLAB_TOKEN=
GIT__SSH_KEY_PATH=
GIT__CLONE_BASE_PATH=./data/repos
GIT__CLONE_TIMEOUT=600
GIT__PULL_TIMEOUT=120
GIT__SSL_VERIFY=false

# 说明：以下文件由服务自动写入/读取（非环境变量），与 GIT__CLONE_BASE_PATH 上级目录一致时位于 ./data/
# - repo_registry.json — 远程 git URL 与 canonical 仓库名映射，避免同一仓库重复索引
# - sync_schedules.json — 定时同步（git pull + 增量索引）计划
# 详见 README「仓库注册表」「定时自动同步」

# API Authentication (optional, leave empty to disable)
API_TOKEN=
