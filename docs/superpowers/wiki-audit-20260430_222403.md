# Wiki 系统深度审计报告 — 2026-04-30

> **审计范围:** 后端 113 个 wiki 模块 + 前端 50+ wiki 组件/hooks + 221 个测试文件  
> **对标项目:** [DeepWiki-Open](https://github.com/AsyncFuncAI/deepwiki-open) (15.3K ⭐)、[CodeWiki](https://github.com/FSoft-AI4Code/CodeWiki) (ACL 2026)  
> **方法:** 3 个并行探索代理 + sequential-thinking 综合分析  
> **最后更新:** 2026-04-30 (全量复审完成；19/19 项已修复)

---

## 1. Executive Summary

KB Service 在**工程化能力**上显著领先竞品（增量更新、多视图、Agent MCP、记忆演化、导出生态均为独有或领先能力）。上轮迭代新增的 DomainComplexityScorer、多模态图表生成、WikiQualityBench 有效缩小了内容生成智能度差距。

原始审计发现 **4 个 P0 正确性问题**、**6 个 P1 体验/可维护性问题**、**9 个 P2 优化项**。经过本轮修复，**全部 19/19 项已解决**。

---

## 2. P0 — 关键正确性问题 (4 个)

### P0-1: quality_gate 中英语言不匹配

- **状态**: ✅ **已修复**
- **修复**: `structural_check` 使用共享标记元组 `_STRUCT_OVERVIEW_MARKERS`、`_STRUCT_COMPONENT_MARKERS`、`_STRUCT_RELATIONSHIP_MARKERS` 同时支持中英文 heading
- **验证**: TDD 测试 `tests/wiki/test_quality_evaluator.py` 显式验证中文标题通过结构检查

### P0-2: 前端域树"批准"不持久化

- **状态**: ✅ **已修复**
- **修复**: `WikiToolPanel` 使用 `useBatchReview()` + `collectDomainApprovalReviews(tree)` 深度遍历域树，调用批量审核 API 后关闭面板

### P0-3: 每域重新生成是空函数

- **状态**: ✅ **已修复**
- **修复**: `WikiDomainReviewPanel` 中 `onRegenerate` 为可选 prop，当未传入时隐藏"重新生成此域"按钮；`WikiToolPanel` 不传 `onRegenerate`

### P0-4: TOPIC 页无法生成语义图表

- **状态**: ✅ **已修复**
- **修复**: `_should_generate` 将 `PageType.TOPIC` 加入 always-generate 集合
- **验证**: TDD 测试 `tests/wiki/test_semantic_diagram_gen.py` 验证 TOPIC 页图表生成

---

## 3. P1 — 重要问题 (6 个)

### P1-1: LangGraph 管道未接入生产路径

- **状态**: ✅ **已修复** (2026-04-30)
- **修复内容**:
  - 新建 `wiki/pipeline_orchestrator.py` 桥接 LangGraph → `generate_business_wiki`
  - 移除 feature flag，LangGraph 为唯一管道
  - docstring 已更新反映 LangGraph 步骤
  - 15 个 orchestrator 单元/集成测试

### P1-2: WikiTopicContent 缺少 businessId 传递

- **状态**: ✅ **已修复**
- **修复**: `WikiTopicContent` 接受 `businessId` / `wikiLinkParams` 并透传给 `MarkdownRenderer`

### P1-3: Review mutations 无用户可见错误状态

- **状态**: ✅ **已修复**
- **修复**: `useReviewErrorToast` 集中处理错误，三个 hooks 均使用 toast 提示

### P1-4: Cache invalidation 逻辑缺陷

- **状态**: ✅ **已修复**
- **修复**: `invalidateWikiQueriesForBusiness` 改为匹配所有 `k[0] === "wiki"` 的 query，JSDoc 已更新

### P1-5: service.py 体积 (3161 → 1508 行) + 大量宽泛异常

- **状态**: ✅ **已修复** (2026-04-30)
- **修复**: 拆分为 5 个独立模块，service.py 从 3161 行减少到 1508 行 (减少 52%)
  - `wiki/persistence.py` (387 行) — `WikiPagePersistence`: 页面持久化、代码哈希、引用同步
  - `wiki/tree_linker.py` (401 行) — `WikiTreeLinker`: 域树构建、嵌套树、树查询
  - `wiki/enrichment_coordinator.py` (340 行) — `WikiEnrichmentCoordinator`: 富化触发、后台执行
  - `wiki/flow_writer.py` (109 行) — `BusinessFlowWriter`: 业务流推理、调用链
  - `wiki/page_composer_service.py` (870 行) — `WikiPageComposerService`: 两遍页面组合
  - `wiki/errors.py` — 共享异常类型 (消除循环导入风险)
- **Code Review**: 通过，HIGH/MEDIUM 问题已修复 (直接导入替代间接引用、共享异常提取)
- **提案文档**: `docs/superpowers/PROPOSAL_20260430_235807_service_py_split.md`

### P1-6: DEPLOYMENT.md 与 config.py 默认值不一致

- **状态**: ✅ **已修复**
- **修复**: DEPLOYMENT.md 默认值已与 config.py 对齐 (`true`)

---

## 4. P2 — 优化项 (9 个)

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| P2-1 | `compose_pages_node` 顺序执行无并发 | `wiki/pipeline_nodes.py` | ✅ 已修复 (asyncio.Semaphore(5) + gather) |
| P2-2 | `pipeline_nodes.py` 使用私有方法 `_build_entity_digest` | `wiki/pipeline_nodes.py` | ✅ 已修复 (公有 build_entity_digest) |
| P2-3 | `plan_structure_node` 命名误导 | `wiki/pipeline_nodes.py` | ✅ 已修复 (→ set_review_status_node) |
| P2-4 | `WikiPageReviewBar` 组件未接入生产路径（死代码） | ~~`WikiPageReviewBar.tsx`~~ | ✅ 已删除 |
| P2-5 | i18n 不一致（硬编码中文 + 中英混合） | `WikiTopicTreeNav.tsx`, `WikiDomainReviewPanel.tsx` | ✅ 已修复 (中文 aria-label) |
| P2-6 | 图表生成失败日志级别过低 (debug) | `wiki/semantic_diagram_gen.py` | ✅ 已修复 (log.warning) |
| P2-7 | business wiki 任务失败信息丢失 | `api/routes/wiki_task_routes.py` | ✅ 已修复 (detail 字段) |
| P2-8 | MCP HTTP 路由无 Pydantic 输入校验 | `api/models/wiki_models.py` | ✅ 已修复 (McpToolCallBody 验证) |
| P2-9 | Knowledge graph 面板无加载/错误状态 | `WikiKnowledgeGraph.tsx` | ✅ 已修复 (加载中/加载失败 UI) |

---

## 5. 已知问题验证状态

| Issue | 状态 | 备注 |
|-------|------|------|
| Issue 1: Wiki 页面粒度过细 (P0) | ✅ 已实现 | LangGraph 已接入生产 |
| Issue 2: 跨仓库域分类性能 (P1) | ✅ 已修复 | SSE-first + 轻量合并 + per-repo 降级 |
| Issue 3: HierarchicalDecomposer 超时 (P2) | ⚠️ 已缓解 | 待优化 max_tokens_per_batch |
| Issue 4: Qwen3 思维链慢 (P2) | ❌ 未解决 | 待调查 ai-gateway 参数 |
| Issue 5: _link_pages_to_tree 循环依赖 (P0) | ✅ 已修复 | 从图遍历改为属性查询 |

---

## 6. 竞品对比矩阵 (2026-04-30 最新)

| 维度 | KB Service | DeepWiki | CodeWiki | 判定 |
|------|-----------|----------|----------|------|
| **图表生成** | 6 种确定性 + LLM 多模态 (4 类型)，TOPIC 页已支持 | LLM Mermaid | 多模态综合 | **已追平** |
| **结构规划** | 业务域 LLM 分类 + 层级分解；代码视图仍是机械遍历 | LLM 语义 TOC | DP 入口点驱动 | 业务视图已赶超，代码视图仍落后 |
| **质量保证** | 多维度 Bench + 置信度 + 矛盾 + heal (中英文均支持) | 无 | CodeWikiBench | **架构领先** |
| **内容生成** | 三级策略 + 复杂度自适应 | 每页一次 LLM，叙事性强 | 递归多智能体 | 已缩小差距 |
| **增量更新** | ✅ webhook + diff + scheduler + SSE | ❌ | ❌ | **强大领先** |
| **多视图** | ✅ business_domain / code_structure / overview | ❌ | ❌ | **独有** |
| **Agent MCP** | 16+ 工具 | per-page chat | ❌ | **强大领先** |
| **记忆演化** | ✅ Q&A 循环 + 遗忘曲线 | ❌ | ❌ | **独有** |
| **导出** | Markdown/ZIP/Obsidian/MkDocs/Git/离线包 | 在线查看 | 文件系统 | **强大领先** |

### 仍存在的竞品差距

| # | 差距 | 来源 | 可借鉴方向 |
|---|------|------|-----------|
| 1 | 即时生成体验 | DeepWiki | Quick Start 模式：输入 URL 自动创建 business 并生成 |
| 2 | Per-page RAG Chat | DeepWiki | 将 Ask 功能内嵌到页面右侧 |
| 3 | 递归深化生成 | CodeWiki | 子模块先生成 → 父模块基于子模块综合 |
| 4 | 入口点驱动解构 | CodeWiki | EntityRoleClassifier 增加 ENTRY_POINT 角色 |
| 5 | 代码视图结构规划 | 共同 | WikiStructurePlanner 增加 LLM 语义分组 |
| 6 | 内联代码片段 | CodeWiki | compose prompt 中注入关键方法的代码片段 |

---

## 7. 测试覆盖概况

- **规模**: 1402 个 wiki 测试，项目全局 ~80% 行覆盖率
- **分层**: 大量 unit/component 测试 + API 测试 + 轻量 integration smoke
- **Pipeline**: LangGraph 管道已在生产路径中，含 E2E mock 测试
- **Orchestrator**: `tests/wiki/test_pipeline_orchestrator.py` 新增 15 个测试
- **审计 TDD**: 新增质量评估器中文标题测试、TOPIC 图表生成测试、管道节点审计测试
- **已知 flaky**: `test_business_tree_persist.py::test_domain_and_repo_same_name_distinct_section_uids`

---

## 8. 修复进度总览

| 优先级 | 总数 | 已修复 | 未修复 |
|--------|------|--------|--------|
| P0     | 4    | 4      | 0      |
| P1     | 6    | 6      | 0      |
| P2     | 9    | 9      | 0      |
| **合计** | **19** | **19** | **0** |
